import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'INF_Delivery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
